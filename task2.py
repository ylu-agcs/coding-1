"""
Task: Implement function to generate a set of 2d data points that are uniformly distributed inside a circle (c, r), where c=(x,y) is the center, and r is the radius. Return the dataset as a pandas dataframe
"""

def random_points_in_circle(c: tuple, r: float):
    return
